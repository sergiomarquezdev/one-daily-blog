export const environment = {
  production: true,
  openAiApiKeyBlog: process.env['OPENAI_API_KEY_BLOG'],
  pgHost: process.env['PGHOST'],
  pgDatabase: process.env['PGDATABASE'],
  pgUser: process.env['PGUSER'],
  pgPassword: process.env['PGPASSWORD'],
};
